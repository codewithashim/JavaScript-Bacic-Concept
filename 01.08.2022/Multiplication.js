// const num = parseInt(prompt('Enter The Value Of Multiplication :'));
let subNum = 1;
const num = 5;

for (subNum; subNum <= 10; subNum++) {
  const reuslt = subNum * num;

  console.log(`${num}*${subNum}=${reuslt}`);
}
